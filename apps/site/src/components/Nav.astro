---
// Nav — barra translúcida fixa (véu + blur), mobile-first.
// Sem props: deriva o item ativo sozinho a partir da URL atual.
// Sem link "Contato" — não existe página real para ele.
const { pathname } = Astro.url;

const links = [
  { href: "/", label: "Início" },
  { href: "/procedimentos", label: "Procedimentos" },
  { href: "/estetica", label: "Estética" },
];

function estaAtivo(href: string) {
  if (href === "/") return pathname === "/";
  return pathname === href || pathname.startsWith(`${href}/`);
}
---

<header class="nav">
  <div class="nav__progress" data-nav-progress aria-hidden="true"></div>
  <div class="nav__inner">
    <a href="/" class="nav__brand" aria-label="Dra. Beatriz Lima — início">
      BL
    </a>

    <nav class="nav__links" aria-label="Navegação principal">
      {
        links.map((link) => (
          <a
            href={link.href}
            class="nav__link"
            aria-current={estaAtivo(link.href) ? "page" : undefined}
            data-active={estaAtivo(link.href) ? "true" : undefined}
          >
            {link.label}
          </a>
        ))
      }
    </nav>

    <button
      type="button"
      class="nav__toggle"
      aria-expanded="false"
      aria-controls="nav-mobile"
      data-nav-toggle
    >
      <span class="nav__toggle-bar"></span>
      <span class="nav__toggle-bar"></span>
      <span class="nav__toggle-bar"></span>
      <span class="sr-only">Abrir menu</span>
    </button>
  </div>

  <nav id="nav-mobile" class="nav__mobile" aria-label="Navegação (mobile)" data-nav-mobile hidden>
    {
      links.map((link) => (
        <a
          href={link.href}
          class="nav__mobile-link"
          aria-current={estaAtivo(link.href) ? "page" : undefined}
          data-active={estaAtivo(link.href) ? "true" : undefined}
        >
          {link.label}
        </a>
      ))
    }
  </nav>
</header>

<style>
  .nav {
    position: sticky;
    top: 0;
    z-index: var(--z-sticky);
    background-color: var(--nav-bg);
    background-image: var(--surface-glass-tint);
    -webkit-backdrop-filter: blur(var(--nav-blur)) saturate(var(--glass-saturate));
    backdrop-filter: blur(var(--nav-blur)) saturate(var(--glass-saturate));
    border-bottom: var(--border-width-hairline) solid var(--nav-border);
    box-shadow: var(--surface-glass-inner);
  }

  .nav__inner {
    height: var(--nav-height);
    max-width: var(--container-xl);
    margin-inline: auto;
    padding-inline: var(--space-container-pad);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-inline-md);
  }

  .nav__brand {
    font-family: var(--type-display-font);
    font-weight: var(--font-weight-semibold);
    font-size: var(--font-size-lg);
    letter-spacing: var(--letter-spacing-wide);
    text-decoration: none;
    /* ouro chapado (AA sobre a nav clara translúcida); o metal vivo fica
       reservado à barra de progresso, onde o gradiente respira sem perder
       legibilidade do monograma. */
    background: var(--gradient-gold-text);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    flex-shrink: 0;
  }

  /* Barra de progresso de leitura — fio metálico que preenche conforme
     o scroll. Driver em JS (rAF): animation-timeline: scroll() ainda não
     roda no iOS Safari (device primário da clientela), então progresso
     confiável vem do --nav-progress escrito no scroll. */
  .nav__progress {
    position: absolute;
    left: 0;
    right: 0;
    bottom: -1px;
    height: 2px;
    transform: scaleX(var(--nav-progress, 0));
    transform-origin: 0 50%;
    background: var(--gradient-metal);
    box-shadow: var(--bloom-gold-soft);
    will-change: transform;
  }

  .nav__links {
    display: none;
    gap: var(--space-inline-lg);
    align-items: center;
  }

  .nav__link {
    position: relative;
    font-family: var(--type-eyebrow-font);
    font-size: var(--font-size-xs);
    text-transform: uppercase;
    letter-spacing: var(--type-eyebrow-tracking);
    color: var(--nav-fg);
    text-decoration: none;
    white-space: nowrap;
    padding-block: 8px;
    transition: color var(--duration-base) var(--ease-standard);
  }

  /* fio que desenha da esquerda no hover/ativo (redesign editorial) */
  .nav__link::after {
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    bottom: 2px;
    height: 1px;
    background: var(--border-hairline);
    transform: scaleX(0);
    transform-origin: 0 50%;
    transition: transform var(--duration-base) var(--ease-entrance);
  }

  .nav__link:hover,
  .nav__link:focus-visible {
    color: var(--nav-fg-active);
  }

  .nav__link[data-active="true"] {
    color: var(--nav-fg-active);
  }

  .nav__link:hover::after,
  .nav__link:focus-visible::after,
  .nav__link[data-active="true"]::after {
    transform: scaleX(1);
  }

  .nav__toggle {
    display: inline-flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 5px;
    width: var(--size-6);
    height: var(--size-6);
    background: transparent;
    border: none;
    cursor: pointer;
    padding: 0;
  }

  .nav__toggle-bar {
    display: block;
    width: 100%;
    height: 1.5px;
    background: var(--nav-fg);
    border-radius: var(--radius-none);
  }

  .nav__toggle[aria-expanded="true"] .nav__toggle-bar:nth-child(1) {
    transform: translateY(6.5px) rotate(45deg);
  }
  .nav__toggle[aria-expanded="true"] .nav__toggle-bar:nth-child(2) {
    opacity: 0;
  }
  .nav__toggle[aria-expanded="true"] .nav__toggle-bar:nth-child(3) {
    transform: translateY(-6.5px) rotate(-45deg);
  }

  .nav__mobile {
    display: flex;
    flex-direction: column;
    padding: var(--space-stack-sm) var(--space-container-pad) var(--space-stack-md);
    border-top: var(--border-width-hairline) solid var(--nav-border);
    background-color: var(--nav-bg);
    background-image: var(--surface-glass-tint);
    -webkit-backdrop-filter: blur(var(--nav-blur)) saturate(var(--glass-saturate));
    backdrop-filter: blur(var(--nav-blur)) saturate(var(--glass-saturate));
  }

  .nav__mobile[hidden] {
    display: none;
  }

  .nav__mobile-link {
    font-family: var(--type-eyebrow-font);
    font-size: var(--font-size-sm);
    text-transform: uppercase;
    letter-spacing: var(--type-eyebrow-tracking);
    color: var(--nav-fg);
    text-decoration: none;
    padding-block: var(--space-inline-sm);
    border-bottom: var(--border-width-hairline) solid var(--nav-border);
  }

  .nav__mobile-link:last-child {
    border-bottom: none;
  }

  .nav__mobile-link[data-active="true"] {
    color: var(--nav-fg-active);
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  @media (min-width: 768px) {
    .nav__links {
      display: flex;
    }
    .nav__toggle {
      display: none;
    }
    .nav__mobile {
      display: none !important;
    }
  }
</style>

<script>
  // Toggle nativo do menu mobile — sem framework, sem dependência do BL·Motion
  // (é estrutural, não decorativo; independe de prefers-reduced-motion).
  const botao = document.querySelector<HTMLButtonElement>("[data-nav-toggle]");
  const painel = document.querySelector<HTMLElement>("[data-nav-mobile]");

  botao?.addEventListener("click", () => {
    const aberto = botao.getAttribute("aria-expanded") === "true";
    botao.setAttribute("aria-expanded", String(!aberto));
    if (painel) painel.hidden = aberto;
  });

  // Progresso de leitura — escreve --nav-progress (0→1) no scroll.
  // rAF-throttled; passive. Reduced-motion mantém o fio no fim (estático).
  const barra = document.querySelector<HTMLElement>("[data-nav-progress]");
  if (barra) {
    let tickando = false;
    const atualizar = () => {
      const max = Math.max(
        1,
        document.documentElement.scrollHeight - window.innerHeight,
      );
      const p = Math.min(1, Math.max(0, window.scrollY / max));
      barra.style.setProperty("--nav-progress", String(p));
      tickando = false;
    };
    const aoScroll = () => {
      if (!tickando) {
        tickando = true;
        requestAnimationFrame(atualizar);
      }
    };
    window.addEventListener("scroll", aoScroll, { passive: true });
    window.addEventListener("resize", aoScroll, { passive: true });
    atualizar();
  }
</script>
